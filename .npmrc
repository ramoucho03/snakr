# React 19 (required by Next 16) is newer than some libraries' peer ranges
# (e.g. @vidstack/react still pins @types/react ^18). These packages work at
# runtime with React 19; we relax peer resolution so both local installs and
# the Docker `npm ci` step resolve identically and reproducibly.
legacy-peer-deps=true
